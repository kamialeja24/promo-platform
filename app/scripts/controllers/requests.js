'use strict';

/**
 * @ngdoc function
 * @name promoPlatformApp.controller:RequestsCtrl
 * @description
 * # RequestsCtrl
 * Controller of the promoPlatformApp
 */
angular.module('promoPlatformApp')
  .controller('RequestsCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
